# PS 26102 checkpoint

## Research update — 11 September 2026

The online AI/ML comparison and fresh read-only feature/text-coverage checks are documented in `AI_ML_RESEARCH_AND_DESIGN.md`. Recommended next model experiments: character TF-IDF retrieval and a small, eligible-feature Isolation Forest shadow queue. Retain operational rules, peer-cost evidence and the existing local investigation architecture; defer unvalidated fraud probabilities, GNN/CV claims and autonomous LLM actions. No new model was trained/downloaded and no frozen dataset/evaluation result changed. The next implementation milestone remains the tested read-only adapter and complete work dossier.

## Completed

1. Problem statement and data context reviewed. The complete supplied inventory is 18 exports: three parliamentary cohorts, each with six dataset types.
2. Data preparation release `all-cohorts-2026-09-10-v2` built and verified earlier: 19 prepared tables, including 160,701 works, 1,023 MP terms and 113,691 accepted payment-report rows. Raw data remain unchanged.
3. A/B protocol and transparent baseline/enhanced screening methods frozen before the experiment. Real snapshot queues and wholly synthetic known-truth testing are separate.
4. Real-data comparison completed for all 160,701 works at 5%, 10% and 20% review budgets. Constructed benchmark completed with 1,800 development and 7,200 test examples, 18 case families, 1,000 paired-context bootstrap draws and 20 alternative tie seeds.
5. A second run reproduced all 17 result artifacts and the manifest byte-for-byte. All 71 reproducibility/input/code checks passed. Independent accounting audit passed 117 checks. The 25 known-answer tests passed again after the interruption.
6. Plain five-sheet Excel results workbook saved, formulas checked and every sheet visually reviewed. Final workbook SHA-256 is recorded in `VISUAL_QA.md`.
7. Architecture, API/data contracts, investigator workflow, access controls, rollout gates and a proposed prospective reviewer study documented in `ARCHITECTURE_AND_WORKFLOW.md`.

## What the results support

At 720 reviews per method, A recovers 601 constructed positives and B recovers 610. The gain is nine cases (+0.25 percentage points of recovery). B loses on four alternative tie seeds and displaces some baseline anomaly families. This does not establish robust superiority or real-world fraud accuracy. Preserve baseline operational access, expose enhanced cost/text findings separately, and require supporting evidence and human findings.

## Resumption checks completed

This checkpoint and `evaluation_18/README.md` supersede the older preparation delivery note's historical statement that A/B had not yet been updated. That frozen preparation note is preserved unchanged as part of the earlier release.

The interrupted documentation correction is applied: `audit_results.py` takes its run directory as a positional argument, not `--run`. The final scope-limit workbook preview is inspected. All frozen evaluation-source hashes, input hashes, result hashes, second-run hashes and the independent-audit source fingerprint still match. No method, threshold, seed or result was changed in the resumption.

The valid independent report is `local/independent_audit_v2.json`. The earlier `independent_audit_v1.json` is a truncated serialization attempt, not valid evidence; it is retained but must not be used. All local outputs remain excluded from publication. No upload, push or deployment occurred in this phase.

## Still to implement

The updated application has **not** been built in this testing-and-planning phase. The previous prototype requires an explicit adapter for the new schema and namespaced IDs. Its connection cannot be fixed safely by changing the database path alone.

Next implementation sequence:

1. Add and test a read-only data/API adapter, version checks and complete work dossier.
2. Connect overview, lifecycle, payments, peer/pair evidence, baseline queue and enhanced comparison screens.
3. Add a separate local review/evidence store, state transitions and auditable investigator actions without modifying facts.
4. Test reconciliation, identity handling, source provenance, security, persistence and offline operation.
5. Conduct an authorized supervised shadow pilot. A live randomized reviewer study requires responsible reviewers, evidence, permissions and a prospectively declared design/power assessment.

Do not restart data preparation or retune this experiment merely to continue the prototype. Preserve the versioned release and completed evidence; implement against their documented contracts. No live-study, production security, forecasting or fraud-accuracy claim is currently justified.
